fun main() {
        val n = readLine()!!.toInt()  // number of test cases

        val answers = ArrayList()
        
        repeat(n) { i ->
            val count = readLine()!!.toInt()  // size of array
            val arr = readLine()!!
                .trim()
                .split(" ")
                .map { it.toInt() }
                .toIntArray()

                
        }
}
